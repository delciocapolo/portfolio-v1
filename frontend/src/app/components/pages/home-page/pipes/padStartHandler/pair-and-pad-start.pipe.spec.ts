import { PairAndPadStartPipe } from './pair-and-pad-start.pipe';

describe('PairAndPadStartPipe', () => {
  it('create an instance', () => {
    const pipe = new PairAndPadStartPipe();
    expect(pipe).toBeTruthy();
  });
});
